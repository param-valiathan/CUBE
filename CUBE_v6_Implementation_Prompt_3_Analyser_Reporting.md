# Implementation Prompt 3 of 3: CUBE v6 — Analyser Paradigm Results Reporting

Copy the block below into a fresh Claude Code session (normal, non-plan-mode permissions) in the `d:\CUBE` working directory. This is prompt 3 of 3 in the CUBE v6 sequence — see `CUBE_v6_Implementation_Prompts_README.md` for the full sequence and shared safety contract. **This prompt has a hard prerequisite: prompts 1 and 2 must have already landed and been reviewed.**

---

## Prompt

Implement `d:\CUBE\CUBE_Analyser_Paradigm_Reporting_Plan.md` in full — the new "Paradigm Results" tab in `cube_analyser.py`, its seven sub-views, the shared occupancy-heatmap and arena/region cluster-and-group cross-tab plots, the new one-sample statistical test primitive, and the in-app "About this metric" documentation content. Read the full plan document before starting. Unlike prompts 1 and 2, this prompt touches **only `cube_analyser.py`** — no changes to `cube_core.py` or `cube.py` — so its regression risk profile is different: the concern is not "did the pipeline's output change" but "did adding a new tab break any existing tab or plot mode in an 18,800-line file with a lot of shared state."

### Step 0 — Prerequisite check and checkpoint

Before any other action:
1. **Verify prompts 1 and 2's deliverables actually exist and produce real data to display.** Read `Kinematics_v6_Implementation_Report.md` and `Environmental_Context_v6_Implementation_Report.md`. Then, using the mandatory `CUBE` env Python, run the pipeline on a small fixture with `env_features_enabled=True` (with at least a boundary + 2 regions + 1 object traced) and `kinematic_directedness_enabled=True`, and confirm `session_env_context.json`, `approach_events.csv` (or equivalent bout-CSV columns per Step 7 of the environmental plan), and `*_bout_lengths_hmm_enriched.csv` actually exist with real content for at least one paradigm. **If any are missing or empty, stop and report — this prompt has nothing correct to build a display layer against otherwise.**
2. Confirm the exact current schema of `session_env_context.json` and the enriched bout sidecar by reading the actual files produced in step 1, not by trusting the original plan docs' proposed schema — prompts 1/2 may have deviated (their own reports should say how; verify against real output regardless).
3. Confirm `git status` is clean. Create local git tag `pre-v6-analyser-reporting` at the current commit. Do not push it. State this tag name, plus all inherited tags from prompts 1 and 2, at the top of the implementation report.

### Ground rules (safety-critical — follow exactly)

1. **Verify the environment first**: `"C:\Users\param\anaconda3\envs\CUBE\python.exe" -c "import umap; print('OK')"`. Stop and report on failure.
2. **Re-verify every citation into `cube_analyser.py` against current source before relying on it** — this file is 18,800+ lines and the cited line numbers (`BSOiDApp` ~17609, `CTkTabview` ~17750, sibling tab registration ~17753-17759, `UnbiasedAnalyticsPanel` ~8012, `BehavioralExplorerPanel` ~16706, `run_cluster_statistics` :1331, `pairwise_posthoc`/`pairwise_posthoc_pooled` :1116-1217, `bh_fdr`/`fdr_sig_column` :1027/:1046, `draw_sig_brackets` :3816, `build_top_n_barplot` :3667, `build_volcano_figure` :2475, `build_heatmap_figure` :2619, `build_umap_comparison_figure` :9385, `THEMES`/`T()` :201-265, `PALETTE` :194-199, `_cluster_color`-style helper :3565, `eg_colors` pattern :3711-3716, `_show_placeholder`/`_show_combined_placeholder`) were accurate at planning time but must be re-confirmed now, especially since prompts 1/2 did not touch this file and should not have shifted these line numbers, but verify rather than assume.
3. **New top-level tab, not a modification of any existing tab.** `ParadigmResultsPanel` is additive — do not alter `UnbiasedAnalyticsPanel`, `BehavioralExplorerPanel`, `GroupPredictorPanel`, or any other existing panel's code beyond the one-line sibling-tab registration these new tabs require. If you find you need to change existing shared helper functions (`run_cluster_statistics`, `PALETTE`, `THEMES`/`T()`, `draw_sig_brackets`, etc.) to serve this plan's needs, treat that as a signal to stop and ask, not to refactor freely — those functions have other, existing call sites whose behavior must not change.
4. **The one genuinely new statistical primitive (one-sample test against a reference value, for Novel Object's chance-level test and CPP's zero-delta test) must be wrapped in the same BH-FDR/effect-size reporting convention as the rest of the stats layer**, per the plan's "Statistical testing" section — implement it as a small, self-contained addition alongside (not inside) `run_cluster_statistics`, and verify its output is visually distinguishable on-plot from the existing between-group test output (different bracket/annotation), per this plan's own verification requirement, so a user cannot mistake one for the other.
5. **Occupancy heatmaps are new construction with no direct precedent** — per the plan's effort/risk note. Build the plot function to accept `t: dict` (theme) exactly like every existing plot builder, use a sequential colormap (e.g. `viridis`) distinct in character from the categorical `PALETTE`, and borrow `build_heatmap_figure`'s colorbar/`GridSpec` handling for layout — but this is new code, so give it its own explicit test/QA pass rather than assuming it inherits correctness from what it borrows layout ideas from.
6. **Group labels are user input from the analyser's own existing group-assignment UI** (`group_assignments.json`, read via the existing `get_groups_fn` accessor) — do not build a second, separate group-labeling mechanism for this panel. Every plot in this panel must be group-aware by default using this exact existing accessor.
7. **Longitudinal/paired comparisons (CPP pre/post) reuse the existing "Experimental Design" (Independent/Repeated) + "Group by" + `subject_id` controls** (`cube_analyser.py:8070-8084`) — do not add a second, CPP-specific "is this longitudinal" toggle. If reusing the existing control pair genuinely cannot express what CPP's pre/post pairing needs, stop and report the gap rather than building a parallel mechanism.
8. **Theme/color QA is a required, explicit verification step, not assumed from code review.** Actually launch `cube_analyser.py` (`"C:\Users\param\anaconda3\envs\CUBE\python.exe" cube_analyser.py`) with fixture data loaded, toggle dark/light theme, and visually confirm every new plot renders correctly in both — per `CLAUDE.md`'s general UI-testing requirement ("For UI or frontend changes... use the feature in a browser/GUI before reporting the task as complete"). Also confirm a specific cluster ID's and a specific group's color match between an existing Unbiased Analytics plot and a new Paradigm Results plot from the same loaded session.
9. **Work in phased checkpoints**, roughly: (a) panel scaffold + tab registration + placeholder-mismatch logic + auto-select-on-load, (b) data loaders for `session_env_context.json`/enriched-bout-sidecar/`approach_events.csv` with graceful degradation for missing files, (c) shared occupancy-heatmap builder + shared arena/region cluster-and-group cross-tab (built once, used by all sub-views), (d) the new one-sample stat primitive, (e) Generic sub-view, (f) each remaining paradigm-specific sub-view in turn (Y-Maze → EPM → Discrimination Index → Sociability → Place Preference → Approach/Avoid), (g) in-app "About this metric" content for all sub-views, (h) theme/color QA pass. Commit locally after each checkpoint with a scoped message.
10. **Regression check for this prompt means confirming existing tabs are unaffected**, not a pipeline byte-identical diff (nothing in `cube_core.py`/`cube.py` changes here): load the same fixture data in `UnbiasedAnalyticsPanel`, `BehavioralExplorerPanel`, and `GroupPredictorPanel` before and after this prompt's changes land, confirm identical behavior/output in all three.
11. **Graceful degradation is a first-class requirement, test it explicitly**: load a pre-v6 model/session with no `session_env_context.json` at all, confirm a clear "no environmental data for this session" message appears rather than a crash or empty broken view, across every sub-view.
12. **If you discover a real, pre-existing bug** (including anything in prompts 1/2's landed code that this prompt's data loaders expose, e.g. a schema mismatch between what `compute_session_env_context()` actually writes and what this prompt expects), report it, don't fix it, unless it blocks this specific prompt's own execution — in which case ask before proceeding.
13. **Test-suite integration, if `tests/` exists**: add pytest cases for the new one-sample stat primitive and for the arena/region cross-tab's aggregation logic (both pure-function-adjacent and fixture-able) — GUI panel construction itself is better covered by the manual QA in ground rule 8, note this split explicitly rather than skipping tests silently.
14. **Never touch real user data or `CUBE_logs/`.**
15. **Do not push to the git remote.**
16. **If you hit a genuine ambiguity** — e.g., exact segmented-button label text, exact placeholder-message wording beyond what the plan's drafted text specifies, or how to lay out the "About this metric" expandable section within `CTkFrame`'s existing layout conventions — pause and ask.

### Required deliverables when finished (or when stopping at whatever checkpoint you reach)

1. All sub-views implemented per the plan's scope (Generic, Y-Maze Alternation, Elevated Plus Maze, Discrimination Index, Sociability/Social Novelty, Place Preference/CPP, Approach/Avoid Events), each with its position/spatial plot, its mandatory control-metric bar (per the plan's literature-check table), its statistical convention, and its in-app documentation content.
2. **A detailed implementation report**, exported as `d:\CUBE\Analyser_Paradigm_Reporting_v6_Implementation_Report.md`, containing:
   - All checkpoint tag names (this prompt's and all inherited ones) at the top.
   - What was actually implemented per checkpoint, with file paths/class/function names.
   - Confirmation that all three pre-existing tabs (Unbiased Analytics, Behavioral Explorer, Group Predictor) were verified unaffected (ground rule 10), and the graceful-degradation case was tested (ground rule 11).
   - Screenshots or a written description of the dark/light theme QA pass (ground rule 8) — actual visual confirmation, not "should work based on using `T()`."
   - Any bugs/inconsistencies found — reported, not fixed.
   - Any deviations from the plan and why, including any place where the actual schema from prompts 1/2's landed output differed from what this plan assumed.
   - Known gaps or anything deliberately left uncovered.
3. **Documentation updates**: update `README.md`, `CUBE_GUIDE.md`, and `GROUP_PREDICTOR_REFERENCE.md` (new tab, new plots, new controls), then run `"C:\Users\param\anaconda3\envs\CUBE\python.exe" d:\CUBE\md_to_docx.py`. `CLAUDE.md` does not need a new entry for this prompt specifically beyond what prompts 1/2 already added, unless a new user-facing default or setting was introduced here that isn't covered by those.
4. **A final plain-text summary message** — completed sub-views, deferred/incomplete pieces, bugs found, checkpoint tag names, and explicit confirmation that existing tabs and the dark/light theme both check out.

---

## Notes for whoever runs this prompt

- This is the final prompt in the CUBE v6 sequence. Once it lands and is reviewed, CUBE v6 as scoped across all three plan documents is complete; anything in the three plans' "What's explicitly deferred" sections (Phase K3, `net_rotation_index`, T-Maze, Morris Water Maze, Option B object tracking, Mode B/feature-fusion clustering, `cube_analyser.py` sub-clustering refinement) remains explicitly out of scope for v6 and should not be started opportunistically "while in the area."
- If this prompt's Step 0 prerequisite check fails (prompts 1/2's output is missing or empty), do not attempt to work around it by fabricating synthetic `session_env_context.json`/sidecar files to develop against — that risks building a display layer against an assumed schema that diverges from what the real pipeline produces. Stop and report instead.
